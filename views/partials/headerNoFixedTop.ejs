<!doctype html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="description" content="">
  <meta name="generator" content="Hugo 0.82.0">
  <title><%=body.title%> · ACED DevOps</title>

  <!-- Favicon -->
  <link rel="icon" href="<%= body.domain %>/favicon.svg" type="image/svg+xml">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Barlow:wght@400;500;600;700&display=swap" rel="stylesheet">


  <style>
    .bd-placeholder-img {
      font-size: 1.125rem;
      text-anchor: middle;
      -webkit-user-select: none;
      -moz-user-select: none;
      user-select: none;
    }

    @media (min-width: 768px) {
      .bd-placeholder-img-lg {
        font-size: 3.5rem;
      }
    }
  </style>

  <!-- Bootstrap core CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">

  <!-- FontAwesome -->
  <!-- <script src="https://kit.fontawesome.com/5f30a8a83b.js" crossorigin="anonymous"></script> -->

  <!-- Stripe JS -->
  <!-- <script src="https://js.stripe.com/v3/"></script> -->


  <!-- Custom styles for this template -->
  <link href="<%= body.domain %>/dist/css/login.css" rel="stylesheet">

</head>
<body class="text-center" style="font-family:'Barlow','Segoe UI','Trebuchet MS',sans-serif;">
<main class="m-auto col-10">
<% if(body.title !== "Account"){%>
  <%if(user){%>
  <div class="mb-5 mt-0 pt-0">
    <a href="" class="btn btn-xs btn-outline-secondary dropdown-toggle"  role="button" id="profileMenu" data-bs-toggle="dropdown" aria-expanded="false">
    <% if(user.photoURL){ %>
      <img src=<%= user.photoURL %> alt="" class="profile-icon">
    <% } else { %>
      <div class="profile-icon bg-secondary">
        <i class="fas fa-1x fa-user mt-2 text-white"></i>
      </div>
      <% } %>

      <%= user.firstName %>
    </a>
    <ul class="dropdown-menu" aria-labelledby="dropdownMenuLink">
      <li><a class="dropdown-item" href="<%= body.domain %>/profile">Acount</a></li>
       <li><a class="dropdown-item" href="<%= body.domain %>/logout">Log Out <i class="fas fa-sign-out-alt"></i></a></li>
     </ul>
  </div>
<%}%>
<%} else {%>
  <a href="<%= body.domain %>/" class="btn btn-xs btn-outline-secondary "><i class="fas fa-home"></i> Home</a>
<%}%>


    <!-- *******************DOMAIN HANDLING*************************** -->
    <span class="d-none" id="domain" domain="<%= body.domain %>">
    </span>